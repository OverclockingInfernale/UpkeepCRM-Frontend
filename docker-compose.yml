services:
  nuxt-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    #image: my-nuxt-app
    ports:
      - "3000:3000"
    environment:
      - AUTH_BASE_URL
      - API_URL
      - API_URL_TOKEN
      - HOST
      - PORT
      - NEXTAUTH_SECRET
